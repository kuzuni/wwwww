# 포지마스터 클론 — 남은 작업 목록

작업 규칙: 한 세션(런)에 **하나의 작업만** 진행한다. 완료하면 체크박스를 채우고 커밋·푸시한다.
시작 전 반드시 `git pull --rebase origin main` 으로 최신화한다.

## 제약 (절대 어기지 말 것)
- three.js r128, 클래식 `<script>` 태그 방식 (모듈/빌드 없음)
- `file://` 더블클릭 실행이 항상 동작해야 함 — 외부 CDN·fetch 금지, 에셋은 base64 임베드
- 밸런스 수치는 `web/BALANCE.md`와 `web/data/raw/` 실측치 기준
- 변경한 js는 `node --check`로 문법 검증, GLB 추가 시 `node web/tools/gen-models-data.js`로 재생성
- 디버그 훅: `?tab=`, `?debug=craft|gear|pets|dungeon|tech|mount|ascend`, `?enemy=slime|golem|goblin|bat|mushroom|wolf|imp`

## 작업 목록 (위에서부터 순서대로)
- [x] 장신구 5부위 3D 프리뷰/썸네일 (부위당 3종 변형)
- [x] Skeleton Mage/Rogue GLB 임베드 — mushroom→Mage, imp→Rogue
- [x] 던전 4종 구현 (Hammer Thief 해머/코인 · Ghost Town 스킬티켓 · Invasion 펫알 · Zombie Rush 블러드) — 열쇠 2/2 자정 리셋, 소탕, `?debug=dungeon[&d=id]`
- [x] 기술 트리(대장장이 연구) 시스템 — BALANCE.md 스펙 기반. 3분기(대장간/힘/스킬&펫) × 노드당 25업(5업×5티어), 재화는 블러드. 메뉴 탭 → 🔬 기술 트리. `?debug=tech`
- [x] 마운트(탈것) 시스템 기초
- [x] 승천(프레스티지) 시스템 — BALANCE.md 스펙 기반
- [x] 사운드: WebAudio 프로시저럴 효과음 (타격/제작 완료/레벨업/뽑기) — 외부 파일 금지
- [x] GLB 모드에서 투구/갑옷 외형 반영 (Knight 고정 갑옷 문제) — 색 오버레이 방식

### 🔥 기획 수정 (사용자 지시 2026-08-16 — 원본 포지마스터와 동일하게. 아래 순서대로, 최우선)

**📐 모든 UI 레이아웃은 `web/ref/UI-SPEC.md`(사용자 제공 스크린샷 20장 분석) 를 기준으로 한다. 아래 작업들 구현 시 반드시 먼저 읽을 것. 기존 클론 UI와 다르면 스펙 쪽이 맞다.**
- [x] **디버그 탭**: 하단 탭바 오른쪽 끝에 🐞 디버그 탭 추가 (출시용 아님, 사용자 테스트용) — ① 스테이지 이동 (챕터-스테이지 입력 or +1/-1 버튼) ② 모든 재화 +100000 버튼 (해머/코인/티켓/물약/태엽/알 등 각각) ③ 대장간 레벨 +1 ④ 던전 열쇠 리필. 항상 노출돼도 됨
- [x] **오프라인 보상 버튼**: 전투 화면 **왼쪽 상단**에 오프라인 보상 버튼 배치 — 누르면 현재 누적된 오프라인 보상 수령(마지막 수령 시각부터 계산), 기존 접속 시 자동 모달과 병행
- [x] **재화 개편**: ① 젬(다이아) 보급 전면 제거 — 첫클리어 보상 등 모든 젬 지급 삭제 (젬 소비 기능은 남겨도 되나 수급처 없음) ② 기술트리 재화를 블러드 → **물약(포션)** 으로 교체 ③ 탈것 뽑기 재화 **태엽(클록와인더)** 신설 (마운트 소환 = 태엽 50개/회, data/raw/mounts.js 참고) ④ 던전 보상 재편: Hammer Thief=해머·코인 / Ghost Town=스킬티켓 / Invasion=펫 알 / Zombie Rush=물약, 태엽은 원본 수급처를 data/raw·BALANCE.md에서 확인해 배치 (원본 확인 불가 시 던전 부가 보상으로 근사) ⑤ 상단바·UI 재화 표기 갱신
  - 구현 메모: ③은 기존 `winders`(태엽/클록와인더)가 이미 소환 50개/회로 스펙과 일치해 신규 구현 없이 표기만 "와인더"→"태엽"로 통일. ④ 태엽 원본 수급처는 data/raw·BALANCE.md에 명시 없어(원본 확인 불가) 기존 스테이지 클리어 지급(첫클리어 +10, 매 클리어 +3)을 근사 수급처로 유지, 던전 보상은 4종 기존 매핑 그대로(중복 없음). 물약은 기존 블러드를 `S.potions`로 리네임(좀비 러시 던전 보상 유지). 젬은 초기값 0 + stageClear 지급分 삭제, 소비처(포지/부화 스킵, 프리미엄 소환)는 유지.
- [ ] **펫 개편**: ① 펫 전투 공격 완전 제거 (펫이 때리지 않음) ② 장착(출전) 시 **고정 수치**로 데미지·체력 증가 (petStats의 원본 수치 참고) ③ 모든 펫에 **옵션 2개** 부여 — 옵션 체계는 장비 서브스탯과 동일 풀(공격속도 +%, 치명타율, 치명타뎀, 공%, 체%, 더블어택 등 원본 포지마스터와 동일) ④ 레벨업 = **다른 펫 합성으로만** (합성 시 레벨↑, 레벨 따라 고정 데미지·체력 상승 — 원본 커브 data/raw 참고) ⑤ 전투 화면 펫 3D는 따라다니는 연출만 유지
- [ ] **탈것 개편**: 펫과 동일 구조 — ① 태엽으로 뽑기 (mountSummonRates 원본 확률) ② 장착 시 고정 데미지·체력 + **옵션 2개** (장비와 동일 옵션 풀) ③ 다른 탈것 합성으로 레벨업, 레벨 따라 스탯 상승 (원본 스탯부스트 +10%~+400% 커브 참고하되 고정치 환산)
- [ ] **스킬 개편**: ① 스킬 데미지를 퍼센트(공격력 배율) → **고정 데미지**로 변경 (레벨 따라 상승, 스킬 상세 팝업에 "각각 7.12k의 피해" 식 표기) ② 스킬 **장착 패시브** 추가 — 장착만 해도 고정 데미지·체력 증가, 스킬 화면 상단에 합계 배너 "+X 기본 피해 +Y 기본 체력" ③ 업글 방식 = **조각 시스템**: 중복 획득 → 조각 적립(게이지 N/8), 조각 소모해 레벨업 (저레벨 1/2 → 고레벨 N/8로 필요량 증가). [모두 업그레이드] [빠른 장착] 버튼 (UI-SPEC 8·11번)
- [ ] **서브스탯 풀 교체**: 장비·펫·탈것 옵션을 UI-SPEC의 원본 13종 풀로 교체 — 치명타 확률(1~12%)/치명타 피해(1~80%)/블록 확률(1~5%)/체력 재생(1~4%)/생명력 흡수(1~20%)/더블 찬스(1~20%)/피해(1~15%)/근접 피해(1~50%)/원거리 피해(1~15%)/공격 속도(1~40%)/스킬 피해(1~30%)/스킬 쿨감(-1~-7%)/체력(1~15%). 전투 계산에 블록/재생/흡혈/근접·원거리·스킬 피해 반영 추가
- [ ] **승천 개편**: 전체 리셋형 승천 폐기 → **개별 승천(별)** 로 교체. 장비/스킬/펫/탈것 각각 승천 가능, 승천 1회 = ⭐1개 (별 수 = 승천 횟수), 카드에 별 표시 (UI-SPEC '승천(별) 시스템' 섹션)
- [ ] **메인 화면 개편** (UI-SPEC 1번): 상단바 = 좌측 프로필 카드(아바타+이름+⚔️전투력) / 우측 👑코인+◆젬. 하단 시트 = 장착 장비 그리드(카드에 Lv+별) + **모루 그림 = 장비 제작 버튼**(탭하면 제작, 아래 🔨보유량) + [대장간 레벨 N] 버튼 + [자동] 버튼. 하단 탭 5개 재구성: 💀던전 / 🚪방 / 🧪소환(스킬·펫·기술트리 서브탭) / 🚩전투(리그) / 🏪상점 (+ 🐞디버그 유지)
- [ ] **대장간 팝업 3종** (UI-SPEC 21~24번): ① [대장간 레벨] 버튼 → 확률 정보 팝업 (시대 10종 현재/다음 레벨 확률 비교 색 막대, 업그레이드 진행 바+젬 스킵) ② ⓘ → 모든 장비 목록 (시대별 아이콘 그리드+개별 확률) ③ 아이템 클릭 → 장비 상세 (스탯+서브스탯 풀 안내) ④ [자동] → 자동 제련 팝업 (유지 시대 체크, 옵션 필터, **망치 1~22개** 스피너, 목표 발견 시 계속 여부, 시작)
- [ ] **던전 개편** (UI-SPEC 6~7번): ① 열쇠 리셋 자정 → **매일 09:00** ② 열쇠 소모 시점 입장 → **완료 시** ③ 던전 팝업 = 하단 탭에서 열림, 가로 배너 4개(일러스트+🔑n/2+열기) ④ 상세 팝업: 난이도 ◀▶ 선택(최고+1 기본, 낮은 단계 재도전 가능), 보상 미리보기, [이전 스테이지 소탕][입장]
- [ ] **소환 탭 통합** (UI-SPEC 8~16번): 스킬/펫/기술트리를 하단 서브탭으로 묶기. 펫 화면 = 알 게이지 + 펫 그리드 + [소환 x1 🥚100] + 부화장 3슬롯(타이머, 젬으로 슬롯+1) + 소환 레벨 확률 팝업(등급 6색 막대). 펫 업그레이드 팝업 = 대상 카드 + 경험치 바 + 합칠 펫/알 선택 → 경험치 흡수
- [ ] **기술트리 개편** (UI-SPEC 10·15~16번): 분기 4개 카드(대장간/힘/스킬·펫&기술/ANIMALS, 진행률%) → 분기 상세 = 세로 노드 트리(노드 n/5). 연구 = **물약 소모 + 실시간 시간 소요**(대장간처럼), 진행 중 노드에 남은 시간, 젬 건너뛰기. 노드 팝업 = 효과 설명 + 진행 바 + [건너뛰기 ◆N][취소]
- [ ] **PvP 리그** (UI-SPEC 3~5번, 봇 기반 오프라인 구현): 플래티넘 리그 랭킹(봇 20명, 닉네임·전투력·⭐점수 생성), 도전 티켓 5/일(09:00 보충), 상대 5명 선택(⭐+1~+5), 전투력 비교+랜덤 승패, 시즌 3일 종료 시 순위 보상(재화 6종 테이블)
- [ ] **진행 패스** (UI-SPEC 18번): 스테이지 도달 마일스톤 무료/프리미엄 2트랙, 무료만 실지급, 프리미엄은 잠금 표시(더미)
- [ ] **상점 탭** (UI-SPEC 17번): 오늘의 특가 3종(기술/펫/탈것 거래) + 보석 패키지 — 결제는 더미(토스트), 배치만 원본대로
- [ ] **프로필/설정 팝업** (UI-SPEC 19~20번): 프로필(이름 편집, 아바타 이모지 선택) + 설정(음악/사운드 토글 실동작, 나머지 더미 토글)
- [ ] **플레이어 정보 팝업** (UI-SPEC 27번): 메인 화면 왼쪽 하단 "!" 버튼 → 프로필 헤더(전투력·대장간 Lv·총 피해·총 체력) + 장착 장비 그리드 + 장착 스킬·펫 행 + **전체 옵션 합계 리스트**(모든 서브스탯 합산)
- [ ] 펫 GLB 전환 검토 (Quaternius Animals CC0, poly.pizza) — 25종 중 대표 몇 종만

## 보류 (로컬 실기기 확인 필요 — 클라우드에서 하지 말 것)
- 무기 본 부착 방향/크기 실기기 확인
